<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abin.checkrepeatsystem.mapper.KnowledgeCategoryMapper">

    <resultMap id="CategoryWithCount" type="com.abin.checkrepeatsystem.pojo.entity.KnowledgeCategory">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="icon" column="icon"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="articleCount" column="article_count"/>
    </resultMap>

    <select id="listWithCount" resultMap="CategoryWithCount">
        SELECT c.*, COUNT(a.id) AS article_count
        FROM knowledge_category c
        LEFT JOIN knowledge_article a ON a.category_id = c.id
            AND a.status = 'PUBLISHED'
            AND a.is_deleted = 0
        WHERE c.is_deleted = 0
        GROUP BY c.id
        ORDER BY c.sort ASC, c.id ASC
    </select>

</mapper>