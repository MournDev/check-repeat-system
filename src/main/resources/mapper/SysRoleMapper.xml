<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abin.checkrepeatsystem.mapper.SysRoleMapper">

    <!-- 根据角色编码查询角色 -->
    <select id="selectByRoleCode" resultType="com.abin.checkrepeatsystem.pojo.entity.SysRole">
        SELECT *
        FROM sys_role
        WHERE role_code = #{roleCode}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <!-- 批量删除角色（软删除） -->
    <update id="batchDeleteRoles">
        UPDATE sys_role
        SET is_deleted = 1,
            update_time = NOW()
        WHERE id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        AND is_deleted = 0
    </update>

    <!-- 更新角色状态 -->
    <update id="updateRoleStatus">
        UPDATE sys_role
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{roleId}
          AND is_deleted = 0
    </update>

    <!-- 查询角色关联的用户数量 -->
    <select id="countUsersByRoleId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM sys_user
        WHERE role_id = #{roleId}
          AND is_deleted = 0
    </select>

</mapper>